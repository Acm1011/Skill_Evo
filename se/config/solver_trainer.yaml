hydra:
  searchpath:
    - file://verl/trainer/config

defaults:
  - ppo_trainer
  - _self_

data:
  gen_batch_size: ${data.train_batch_size}
  

reward_model:
  reward_manager: solver
  reward_kwargs:
    storage_path: /root/users/ycy/Self-evolving-Agent/saved_results/solver
    filter_lower: 0.0
    filter_high: 1.0
    

algorithm:
  filter_groups:
    _target_: verl.trainer.config.FilterGroupsConfig
    enable: False # We try to avoid forgetting to set enable
    metric: null # acc / score / seq_reward / seq_final_reward / ...
    max_num_gen_batches: 0 # Non-positive values mean no upper limit
    filter_lower: 0.0
    filter_high: 1.0

trainer:
  project_name: verl-solver
